interface Builder {
    andPath(data: string): TheConfig;
}
export declare class TheConfig {
    private filePath;
    private data;
    constructor(filePath: string, data: string);
    static fromFile(filePath: string): Builder;
    retrieveData(): any;
}
export {};
